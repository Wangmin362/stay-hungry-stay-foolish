package _0_basic

import (
	"fmt"
	"testing"
)

// https://leetcode.cn/problems/isomorphic-strings/

func isIsomorphic(s string, t string) bool {
	cnt := func(str string) []int {
		arr := make([]int, 26)
		for _, s := range str {

		}
	}
}

func TestIsIsomorphic(t *testing.T) {
	fmt.Println(isIsomorphic("egg", "add"))
}
